---
import Layout from "../layouts/Layout.astro";
---

<Layout pageTitle="Contact — Dana Sandy" pageDescription="Reach out about product strategy, AI experimentation, the SKAI series, or just to say hello.">

  <div class="contact-body">

    <!-- TWO-COLUMN: text + illustration on left, form on right -->
    <section class="contact-layout">

      <div class="contact-intro">
        <p class="eyebrow">Let's talk</p>
        <h1 class="contact-title">Get <em>in touch</em></h1>
        <p class="contact-lede">Whether it's a product collaboration, an AI experiment, an update on the SKAI series, or just a hello — I'd love to hear from you.</p>
        <div class="contact-illu" aria-hidden="true">
          <picture>
            <source srcset="/dana-contact.webp" type="image/webp" />
            <img src="/dana-contact.png" alt="" loading="lazy" decoding="async" width="900" height="900" />
          </picture>
        </div>
        <p class="form-aside">Prefer email? <a class="info-link" href="mailto:hello@danasandy.com">hello@danasandy.com</a></p>
      </div>

      <div class="contact-form-wrap">
        <div class="form-success" id="form-success" hidden>
          <p class="eyebrow">Thank you</p>
          <h2 class="success-title">Message <em>received</em>.</h2>
          <p class="success-text">I'll get back to you soon. In the meantime, you can also reach me at <a class="info-link" href="mailto:hello@danasandy.com">hello@danasandy.com</a>.</p>
        </div>

        <form
          class="contact-form"
          id="contact-form"
          name="contact"
          method="POST"
          data-netlify="true"
          netlify-honeypot="bot-field"
          action="/contact?success=1"
        >
          <input type="hidden" name="form-name" value="contact" />
          <p class="hp-field"><label>Don't fill this out: <input name="bot-field" /></label></p>

          <div class="field">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" autocomplete="name" required />
          </div>

          <div class="field">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" autocomplete="email" required />
          </div>

          <div class="field">
            <label for="subject">Subject</label>
            <select id="subject" name="subject" required>
              <option value="" disabled selected>Choose one</option>
              <option value="SKAI Series updates">SKAI Series updates</option>
              <option value="Collaboration">Collaboration</option>
              <option value="Product Management inquiry">Product Management inquiry</option>
              <option value="General hello">General hello</option>
            </select>
          </div>

          <div class="field">
            <label for="message">Message</label>
            <textarea id="message" name="message" rows="6" required></textarea>
          </div>

          <button type="submit" class="form-submit">Send message &rarr;</button>
        </form>
      </div>

    </section>

  </div>

</Layout>

<script>
  function initContactPage() {
    if (window.location.search.includes('success=1')) {
      const form = document.getElementById('contact-form');
      const success = document.getElementById('form-success');
      if (form) form.style.display = 'none';
      if (success) success.hidden = false;
    }
  }
  document.addEventListener('astro:page-load', initContactPage);
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initContactPage);
  } else {
    initContactPage();
  }
</script>

<style>
  .contact-body {
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 72px;
  }

  /* ─── TWO-COLUMN LAYOUT ─── */
  .contact-layout {
    padding: 140px 0 120px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: start;
  }

  /* ─── INTRO COLUMN (text + illustration + email) ─── */
  .contact-intro {
    display: flex;
    flex-direction: column;
  }

  .contact-illu {
    margin-top: 36px;
    width: 100%;
    max-width: 460px;
    filter: drop-shadow(0 0 32px rgba(200, 164, 90, 0.18));
    animation: illuFloat 7s ease-in-out infinite;
  }

  .contact-illu img {
    width: 100%;
    height: auto;
    display: block;
  }

  @keyframes illuFloat {
    0%, 100% { transform: translateY(0); }
    50%      { transform: translateY(-12px); }
  }

  .eyebrow {
    font-family: var(--font-body);
    font-weight: var(--weight-regular);
    font-size: var(--text-2xs);
    letter-spacing: 0.45em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 18px;
  }

  .contact-title {
    font-family: var(--font-display);
    font-weight: var(--weight-light);
    font-size: clamp(48px, 6.5vw, 80px);
    line-height: 0.95;
    letter-spacing: var(--tracking-tight);
    color: var(--cream);
    margin-bottom: 24px;
  }

  .contact-title em {
    font-style: italic;
    color: var(--gold-light);
  }

  .contact-lede {
    font-family: var(--font-display);
    font-style: italic;
    font-size: clamp(18px, 1.9vw, 22px);
    line-height: var(--leading-relaxed);
    color: var(--cream);
  }

  .form-aside {
    margin-top: 28px;
    font-family: var(--font-display);
    font-style: italic;
    font-size: var(--text-base);
    color: var(--text-muted);
  }

  .info-link {
    color: var(--gold-light);
    text-decoration: none;
    border-bottom: 2px solid var(--gold);
    padding: 0 0 3px;
    transition: color 0.25s, border-color 0.25s;
  }

  .info-link:hover {
    color: var(--cream);
    border-color: var(--cream);
  }

  /* ─── FORM COLUMN ─── */
  .contact-form-wrap {
    width: 100%;
    max-width: 540px;
    justify-self: end;
  }

  /* ─── FORM ─── */
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 22px;
  }

  /* Honeypot — visually hidden but accessible */
  .hp-field {
    position: absolute !important;
    left: -10000px !important;
    top: auto;
    width: 1px;
    height: 1px;
    overflow: hidden;
  }

  .field {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .field label {
    font-family: var(--font-body);
    font-weight: var(--weight-medium);
    font-size: var(--text-2xs);
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--gold);
  }

  .field input,
  .field select,
  .field textarea {
    font-family: var(--font-body);
    font-size: var(--text-base);
    color: var(--cream);
    background: rgba(26, 34, 20, 0.6);
    border: 1px solid rgba(200, 164, 90, 0.25);
    border-radius: 2px;
    padding: 14px 16px;
    outline: none;
    transition: border-color 0.25s, background 0.25s, box-shadow 0.25s;
    width: 100%;
  }

  .field input::placeholder,
  .field textarea::placeholder {
    color: rgba(245, 240, 232, 0.4);
  }

  .field input:hover,
  .field select:hover,
  .field textarea:hover {
    border-color: rgba(200, 164, 90, 0.45);
  }

  .field input:focus,
  .field select:focus,
  .field textarea:focus {
    border-color: var(--gold-light);
    background: rgba(26, 34, 20, 0.85);
    box-shadow: 0 0 0 3px rgba(200, 164, 90, 0.12);
  }

  .field select {
    appearance: none;
    -webkit-appearance: none;
    background-image: linear-gradient(45deg, transparent 50%, var(--gold) 50%),
                      linear-gradient(135deg, var(--gold) 50%, transparent 50%);
    background-position: calc(100% - 22px) calc(50% + 2px), calc(100% - 16px) calc(50% + 2px);
    background-size: 6px 6px, 6px 6px;
    background-repeat: no-repeat;
    cursor: pointer;
    padding-right: 36px;
  }

  .field select:focus {
    background-image: linear-gradient(45deg, transparent 50%, var(--gold-light) 50%),
                      linear-gradient(135deg, var(--gold-light) 50%, transparent 50%);
    background-position: calc(100% - 22px) calc(50% + 2px), calc(100% - 16px) calc(50% + 2px);
    background-size: 6px 6px, 6px 6px;
    background-repeat: no-repeat;
  }

  .field select option {
    background: var(--olive-dark);
    color: var(--cream);
  }

  .field textarea {
    resize: vertical;
    min-height: 160px;
    font-family: var(--font-body);
    line-height: var(--leading-normal);
  }

  .form-submit {
    margin-top: 8px;
    align-self: flex-start;
    font-family: var(--font-body);
    font-weight: var(--weight-medium);
    font-size: var(--text-sm);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--olive-dark);
    background: var(--gold);
    border: none;
    padding: 14px 32px;
    border-radius: 2px;
    cursor: pointer;
    transition: background 0.25s, letter-spacing 0.3s, transform 0.2s;
  }

  .form-submit:hover {
    background: var(--gold-light);
    letter-spacing: 0.26em;
  }

  .form-submit:active {
    transform: translateY(1px);
  }

  /* ─── SUCCESS ─── */
  .form-success {
    padding: 32px 0;
  }

  .success-title {
    font-family: var(--font-display);
    font-weight: var(--weight-light);
    font-size: clamp(36px, 5vw, 56px);
    line-height: var(--leading-snug);
    color: var(--cream);
    margin: 16px 0 20px;
  }

  .success-title em {
    font-style: italic;
    color: var(--gold-light);
  }

  .success-text {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    line-height: var(--leading-loose);
    color: var(--cream);
    max-width: 540px;
  }

  /* ─── RESPONSIVE ─── */
  @media (max-width: 1000px) {
    .contact-body {
      padding: 0 24px;
    }
    .contact-layout {
      padding: 100px 0 80px;
      grid-template-columns: 1fr;
      gap: 48px;
    }
    .contact-illu {
      max-width: 320px;
      align-self: center;
      margin-top: 24px;
    }
    .contact-form-wrap {
      max-width: 100%;
      justify-self: stretch;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .contact-illu {
      animation: none;
    }
  }
</style>
</content>
</invoke>