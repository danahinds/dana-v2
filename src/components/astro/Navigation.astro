<nav>
  <div class="nav-left">
    <a href="https://www.linkedin.com/in/danasandy" target="_blank" rel="noopener noreferrer" class="linkedin-link" aria-label="LinkedIn">
      <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
    </a>
  </div>

  <div class="nav-center">
    <a href="/"><span class="cap">D</span>ana <span class="cap">S</span>andy</a>
  </div>

  <ul class="nav-links">
    <li><a href="/">Home</a></li>
    <li><a href="/about">About</a></li>
    <li><a href="/posts/1">SKAI Series</a></li>
  </ul>

  <div class="nav-right">
    <a href="/contact" class="btn btn-primary">Work with me</a>
  </div>
</nav>

<style>
  nav {
    background-color: var(--olive-dark);
    padding: 3.5rem 3rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    border-bottom: 1px solid var(--border);
  }

  nav * {
    align-self: center;
  }

  .nav-left {
    flex: 0 0 auto;
  }

  .linkedin-link {
    color: var(--cream);
    text-decoration: none;
    display: flex;
    align-items: center;
    transition: color 0.3s ease;
  }

  .linkedin-link:hover {
    color: var(--gold);
  }

  .nav-center {
    flex: 0 0 auto;
    font-family: var(--font-display);
    font-size: 1.75rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    font-kerning: normal;
    font-feature-settings: "kern" 1, "liga" 1;
  }

  .nav-center a {
    color: var(--gold);
    text-decoration: none;
  }

  .nav-center .cap {
    font-size: 1.15em;
    font-weight: 600;
  }

  .nav-links {
    display: flex;
    gap: 2rem;
    list-style: none;
    flex: 1;
    justify-content: center;
  }

  .nav-links a {
    font-size: 0.95rem;
    color: var(--cream);
    text-decoration: none;
  }

  .nav-links a:hover {
    color: var(--gold);
  }

  .nav-right {
    flex: 0 0 auto;
  }

  /* Ensure button visibility in nav */
  nav .btn-primary {
    background: var(--cream) !important;
    color: var(--olive-dark) !important;
    border-color: var(--cream) !important;
  }

  nav .btn-primary:hover {
    background: var(--gold) !important;
    color: var(--olive-dark) !important;
    border-color: var(--gold) !important;
  }

  @media (max-width: 1024px) {
    nav {
      flex-wrap: wrap;
      justify-content: center;
      padding: 2rem 1.5rem;
    }

    .nav-left {
      order: 1;
      flex: 0 0 100%;
      justify-content: center;
      display: flex;
      margin-bottom: 1.5rem;
    }

    .linkedin-link svg {
      width: 20px;
      height: 20px;
    }

    .nav-center {
      order: 2;
      flex: 0 0 100%;
      text-align: center;
      margin: 1rem 0 1.5rem;
      font-size: 1.5rem;
    }

    .nav-links {
      order: 3;
      flex: 0 0 100%;
      justify-content: center;
      flex-wrap: wrap;
      gap: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .nav-right {
      order: 4;
      flex: 0 0 100%;
      display: flex;
      justify-content: center;
    }
  }
</style>
