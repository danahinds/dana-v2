<nav>
  <div class="nav-left">
    <a href="/" aria-label="Dana Sandy Home">
      <img src="/ds-monogram.png" alt="Dana Sandy Monogram" class="logo" />
    </a>
  </div>

  <div class="nav-center">
    <a href="/">Dana Sandy</a>
  </div>

  <ul class="nav-links">
    <li><a href="/">Home</a></li>
    <li><a href="/about">About</a></li>
    <li><a href="/posts/1">SKAI Series</a></li>
  </ul>

  <div class="nav-right">
    <a href="/contact" class="btn btn-primary">Work with me</a>
  </div>
</nav>

<style>
  nav {
    background-color: var(--olive-dark);
    padding: 3.5rem 3rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    border-bottom: 1px solid var(--border);
  }

  nav * {
    align-self: center;
  }

  .nav-left {
    flex: 0 0 auto;
  }

  .logo {
    height: 50px;
    width: auto;
    display: block;
    transition: transform 0.3s ease;
  }

  .logo:hover {
    transform: scale(1.05);
  }

  .nav-center {
    flex: 0 0 auto;
    font-family: var(--font-display);
    font-size: 1.25rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .nav-center a {
    color: var(--cream);
  }

  .nav-links {
    display: flex;
    gap: 2rem;
    list-style: none;
    flex: 1;
    justify-content: center;
  }

  .nav-links a {
    font-size: 0.95rem;
    color: var(--cream);
    text-decoration: none;
  }

  .nav-links a:hover {
    color: var(--gold);
  }

  .nav-right {
    flex: 0 0 auto;
  }

  /* Ensure button visibility in nav */
  nav .btn-primary {
    background: var(--cream) !important;
    color: var(--olive-dark) !important;
    border-color: var(--cream) !important;
  }

  nav .btn-primary:hover {
    background: var(--gold) !important;
    color: var(--olive-dark) !important;
    border-color: var(--gold) !important;
  }

  @media (max-width: 1024px) {
    nav {
      flex-wrap: wrap;
      justify-content: center;
      padding: 2rem 1.5rem;
    }

    .nav-left {
      order: 1;
      flex: 0 0 100%;
      justify-content: center;
      display: flex;
      margin-bottom: 1.5rem;
    }

    .logo {
      height: 45px;
    }

    .nav-center {
      order: 2;
      flex: 0 0 100%;
      text-align: center;
      margin: 1rem 0 1.5rem;
      font-size: 1.1rem;
    }

    .nav-links {
      order: 3;
      flex: 0 0 100%;
      justify-content: center;
      flex-wrap: wrap;
      gap: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .nav-right {
      order: 4;
      flex: 0 0 100%;
      display: flex;
      justify-content: center;
    }
  }
</style>
